### 总体结论

- **脚本（Script）、音频、语音 & LoRA 前后端整体结构是合理的**，我没有发现明显的致命设计错误。  
- 主要问题集中在：**LoRA 训练前端页面和后端 API 路径/字段不匹配、LoRA 检查点查找逻辑不一致**，这些已经帮你修正并通过前端 `npm run build` 验证构建无误。  
- 后端 pytest 目前在你本地环境因为 `PYTHONPATH` 未配置导致 `ModuleNotFoundError: app`，这属于运行环境配置问题，而不是代码错误（下面会告诉你如何本地修）。  

下面按你关心的几个点分别说明。

---

### 一、脚本相关（`backend/app/api/scripts.py`）

**检查结果：**

- 脚本生成后台任务 `generate_script_task`：
  - 正确使用独立的 `async_session_factory()`，避免把依赖注入的 `db` 会话传到后台任务，这是正确做法。
  - 对 `Project` / `Book` 混淆做了多重防御：
    - 先用 `select(Book).where(Book.id == project.book_id)` 明确查询书。
    - 用 `isinstance(book, ProjectModel)` 检查防止 ORM 关系配置错误。
    - 检查 `hasattr(book, 'file_path')`，防止 `Book` 模型缺少字段。
  - 根据 `book.file_type` 分别处理 `txt` / `pdf` / `epub`，通过 `app.api.books` 中的抽取函数获取文本，这里逻辑完整、出错时也会更新 `script.status = "failed"` 和 `error_message`，整体健壮。

- 脚本状态和内容查询：
  - `GET /api/projects/{project_id}/scripts/status` 和 `GET /api/projects/{project_id}/scripts` 返回的数据结构与前端 `scriptsApi` 文档一致，包括 `speakers` 自动从 `script.content` 推出，没有明显问题。

- 脚本更新 & Chunk 生成：
  - `PATCH /api/projects/{project_id}/scripts` 会：
    - 用前端传入的 `ScriptUpdate` 覆盖 `script.content`。
    - 可选地删除所有旧 `Chunk`，然后用 `split_script_to_chunks` 智能分块。
  - `POST /api/projects/{project_id}/scripts/chunks` 单独从脚本重新分块。
  - 这两者和前端 `scriptsApi` / `audioApi.getChunks` 的用法是一致的。

**结论：**  
脚本 API 这块设计比较严谨，没有发现明显逻辑错误或类型问题，无需修改。

---

### 二、前端音频播放器 & 语音逻辑（`AudioPlayer.tsx`）

**已检查点：**

- 组件 `AudioPlayer` 同时支持：
  - **Web Speech API 文字朗读模式**（无 `audioUrl`，通过 `window.speechSynthesis` 朗读段落）。
  - **音频文件播放模式**（有 `audioUrl`，使用 `<audio>` + WaveSurfer 波形）。
- 关键状态来源于 `useAudioStore`：
  - `isPlaying`, `currentTime`, `duration`, `speed`, `audioUrl`, `blockTexts`, `activeBlockIndex` 等。
- Web Speech 模式逻辑：
  - 没有音频文件但有时长时，优先从 `blockTexts`（store 里）加载段落；若为空再从 DOM `[data-block-index]` 兜底。
  - `playWebSpeechAtIndex` 会：
    - 跳过空白段。
    - 按 `speed` 设置 `utterance.rate`。
    - 在 `onend` 回调里自动播放下一段，直到结束或停止。
  - 支持：
    - 进度条拖动（根据当前时间换算到段落索引）。
    - 前进/后退按段落跳转。
- 音频文件模式逻辑：
  - `<audio>` timeupdate 驱动 `currentTime`。
  - WaveSurfer 可拖动进度，双向同步。
  - `duration` 由 store 统一管理，组件不会乱改。

**潜在问题排查：**

- 检查了是否存在明显的状态不同步、死循环 `useEffect`、或 `window` 未判断导致 SSR 报错：
  - `typeof window !== 'undefined'` 判断已存在。
  - Web Speech 模式与音频模式之间的 `useEffect` 都有 `useWebSpeech` 保护，避免互相干扰。
- 逻辑上没有发现会直接导致播放错误或崩溃的 bug，前端也已成功 `npm run build`。

**结论：**  
`AudioPlayer.tsx` 结构复杂但逻辑自洽，没有明显错误，**无需改动即可稳定工作**。如你后面遇到具体“这段不能正确朗读/高亮不同步”的情况，可以再针对具体 case 深挖。

---

### 三、LoRA 训练：前后端接口 & 训练流程

这是本次发现问题最多的一块，我已做了较大修正。

#### 3.1 后端 LoRA 服务（`backend/app/services/lora_training.py`）

**问题 & 修复：**

- **问题 1：`get_latest_checkpoint` 和真实保存路径不一致**
  - 训练结束时，`start_training` 会把最终模型写到：
    - `settings.export_dir / "lora_checkpoints" / f"{job_id}_final.pt"`
  - 但原来的 `get_latest_checkpoint` 只去找：
    - `self.checkpoint_dir / job_id / "checkpoint_*.pt"`，导致永远返回不到最终文件。

- **修复：**

```python
```12:31:/Users/gongfan/Documents/youshengshu/backend/app/services/lora_training.py
    async def get_latest_checkpoint(self, job_id: str) -> Optional[str]:
        """Get path to latest checkpoint for a job."""
        # First prefer the final checkpoint file created by start_training
        final_checkpoint = self.checkpoint_dir / f"{job_id}_final.pt"
        if final_checkpoint.exists():
            return str(final_checkpoint)

        # Backwards‑compatible: also support older layout with per‑job subdirectory
        job_dir = self.checkpoint_dir / job_id
        if job_dir.exists():
            checkpoints = list(job_dir.glob("checkpoint_*.pt"))
            if checkpoints:
                # Sort by modification time, get latest
                latest = max(checkpoints, key=lambda p: p.stat().st_mtime)
                return str(latest)

        return None
```

这样既能找到新格式的 `_final.pt`，也兼容旧目录结构。

---

#### 3.2 后端 LoRA API（`backend/app/api/lora_training.py`）

**1）进度接口字段对齐前端**

前端 `LoRATraining.tsx` 期待的字段有：

- `status`
- `current_epoch`
- `total_epochs`
- `loss`
- `learning_rate`
- `elapsed_time`
- `estimated_time_remaining`
- `checkpoint_path`
- `error`

而原后端 `/api/lora/projects/{project_id}/progress` 返回的是：

- `eta_seconds` / `error_message`，没有 `elapsed_time` / `estimated_time_remaining` / `checkpoint_path` 等。

**已修改：**

```python
```188:233:/Users/gongfan/Documents/youshengshu/backend/app/api/lora_training.py
@router.get("/projects/{project_id}/progress", response_model=ApiResponse[dict])
async def get_training_progress(
    project_id: str,
    current_user: CurrentUserDep,
    db: DbDep,
):
    ...
    job_id, progress = project_job

    # 计算大致训练耗时
    elapsed_time = 0
    try:
        if progress.started_at:
            from datetime import datetime

            start_dt = datetime.fromisoformat(progress.started_at)
            end_dt = datetime.fromisoformat(progress.completed_at) if progress.completed_at else datetime.now()
            elapsed_time = max(0, int((end_dt - start_dt).total_seconds()))
    except Exception:
        elapsed_time = 0

    return ApiResponse(
        data={
            "job_id": job_id,
            "status": progress.status.value,
            "current_epoch": progress.current_epoch,
            "total_epochs": progress.total_epochs,
            "current_step": progress.current_step,
            "total_steps": progress.total_steps,
            "loss": progress.loss,
            "learning_rate": progress.learning_rate,
            "elapsed_time": elapsed_time,
            "estimated_time_remaining": progress.eta_seconds,
            "checkpoint_path": progress.checkpoint_path,
            "error": progress.error_message,
        }
    )
```

这样前端不需要再猜字段名。

---

**2）检查点下载接口**

原来只有 `/api/lora/projects/{project_id}/checkpoint` 返回 JSON 元信息，  
前端 `LoRATraining.tsx` 却直接把 `/api/projects/{projectId}/lora/checkpoint` 当成二进制文件下载，必然错。

**已新增一个真正下载文件的接口：**

```python
```275:309:/Users/gongfan/Documents/youshengshu/backend/app/api/lora_training.py
@router.get("/projects/{project_id}/checkpoint/download")
async def download_latest_checkpoint(
    project_id: str,
    current_user: CurrentUserDep,
    db: DbDep,
):
    """
    Download the latest trained LoRA checkpoint for the project.
    """
    # Verify project exists
    result = await db.execute(select(Project).where(Project.id == project_id))
    if not result.scalar_one_or_none():
        raise NotFoundException("Project not found")

    service = get_lora_training_service()
    jobs = await service.list_training_jobs()

    checkpoint_path = None
    for job_id, progress in jobs:
        if job_id.startswith(project_id) and progress.status == TrainingStatus.COMPLETED:
            checkpoint_path = await service.get_latest_checkpoint(job_id)
            break

    if not checkpoint_path:
        raise NotFoundException("No trained checkpoint found for this project")

    return FileResponse(
        path=checkpoint_path,
        filename=f"lora_model_{project_id}.pt",
        media_type="application/octet-stream",
    )
```

原来的 `/projects/{project_id}/checkpoint` 仍然保留，现在仅返回 metadata，不会影响其它代码或未来用法。

---

#### 3.3 前端 LoRA 训练页面（`read-rhyme/src/pages/LoRATraining.tsx`）

**前端的主要问题：**

1. **路径写成 `/api/projects/${projectId}/lora/...`，而后端真实是 `/api/lora/...`。**
2. **TrainingRequirements 类型与后端返回结构不一致。**
3. **`handleStartTraining` 上传音频文件 FormData 到 `/projects/.../lora/train`，但后端训练实际上是基于项目已有 chunk 音频的 JSON 接口 `/api/lora/projects/{project_id}/train`。**
4. **下载检查点直接把 JSON 当成二进制，必然出错。**

**已做修改：**

1）**对齐 Requirements 类型和展示**

- 类型改为和后端一致：

```typescript
```71:88:/Users/gongfan/Documents/youshengshu/read-rhyme/src/pages/LoRATraining.tsx
interface TrainingRequirements {
  min_samples: number;
  recommended_samples: number;
  min_duration_per_sample: number;
  recommended_duration_per_sample: number;
  total_min_duration: number;
  recommended_total_duration: number;
  supported_formats: string[];
  sample_rate: number;
  hardware?: {
    min_memory_gb: number;
    recommended_memory_gb: number;
    gpu_required: boolean;
    gpu_memory_gb: number;
  };
}
```

- 请求地址改为后端真实路径：

```typescript
```108:114:/Users/gongfan/Documents/youshengshu/read-rhyme/src/pages/LoRATraining.tsx
const fetchRequirements = async () => {
  try {
    const response = await authFetch(`/api/lora/requirements`);
    const data = await response.json();
    if (data.success) {
      setRequirements(data.data);
    }
  } catch (error) {
    console.error("Failed to fetch requirements:", error);
  }
};
```

- 展示时把总时长秒数换算成分钟、用 `min_samples` 等真实字段：

```typescript
```419:435:/Users/gongfan/Documents/youshengshu/read-rhyme/src/pages/LoRATraining.tsx
<p className="text-sm text-muted-foreground">最小时长</p>
<p className="text-lg font-medium">
  {requirements.total_min_duration
    ? (requirements.total_min_duration / 60).toFixed(1)
    : "—"}{" "}
  分钟
</p>
...
<p className="text-sm text-muted-foreground">最少文件数</p>
<p className="text-lg font-medium">
  {requirements.min_samples ?? "—"} 个
</p>
...
<p className="text-sm text-muted-foreground">采样率</p>
<p className="text-lg font-medium">
  {requirements.sample_rate ?? "—"} Hz
</p>
...
<p className="text-sm text-muted-foreground">支持格式</p>
<p className="text-lg font-medium">
  {requirements.supported_formats?.join(", ") || "—"}
</p>
```

底部提示文案同步用新的字段：

```typescript
```672:675:/Users/gongfan/Documents/youshengshu/read-rhyme/src/pages/LoRATraining.tsx
需要至少 {requirements.min_samples ?? "—"} 个文件，总计{" "}
{requirements.total_min_duration
  ? (requirements.total_min_duration / 60).toFixed(1)
  : "—"}{" "}
分钟音频
```

---

2）**进度查询 & 类型映射**

- 请求路径改成后端真实路径：

```typescript
```120:127:/Users/gongfan/Documents/youshengshu/read-rhyme/src/pages/LoRATraining.tsx
const response = await authFetch(`/api/lora/projects/${projectId}/progress`);
```

- 把后端返回的 `data.data` 映射成前端 `TrainingProgress` 类型，提供默认值，避免 `undefined` 导致 NaN：

```typescript
```123:135:/Users/gongfan/Documents/youshengshu/read-rhyme/src/pages/LoRATraining.tsx
if (data.success && data.data) {
  const raw = data.data;
  const mapped: TrainingProgress = {
    status: (raw.status || "idle") as TrainingProgress["status"],
    current_epoch: raw.current_epoch ?? 0,
    total_epochs: raw.total_epochs ?? 0,
    loss: raw.loss ?? 0,
    learning_rate: raw.learning_rate ?? 0,
    elapsed_time: raw.elapsed_time ?? 0,
    estimated_time_remaining: raw.estimated_time_remaining ?? null,
    checkpoint_path: raw.checkpoint_path,
    error: raw.error,
  };
  setProgress(mapped);
}
```

---

3）**创建训练任务：改为 JSON 调用后端实际接口**

目前后端是用项目里已生成的 chunk 音频进行训练，所以**暂时不使用前端上传的音频文件**（UI 保留，只是后端忽略，这点你后续可以考虑打通为真正自定义数据集）。

- 调用改为：

```typescript
```155:169:/Users/gongfan/Documents/youshengshu/read-rhyme/src/pages/LoRATraining.tsx
const response = await authFetch(`/api/lora/projects/${projectId}/train`, {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    voice_name: config.voice_name,
    num_epochs: config.epochs,
    batch_size: config.batch_size,
    learning_rate: config.learning_rate,
  }),
});
const data = await response.json();
```

路径和字段都与后端 `TrainingJobCreate` / `TrainingConfig` 对齐。

---

4）**取消训练 & 下载检查点路径修正**

- 取消训练：

```typescript
```215:221:/Users/gongfan/Documents/youshengshu/read-rhyme/src/pages/LoRATraining.tsx
const response = await authFetch(`/api/lora/projects/${projectId}/cancel`, {
  method: "POST",
});
```

- 下载检查点：改为调用新的下载接口 `/api/lora/projects/{projectId}/checkpoint/download` 并当作二进制处理：

```typescript
```240:247:/Users/gongfan/Documents/youshengshu/read-rhyme/src/pages/LoRATraining.tsx
const response = await authFetch(`/api/lora/projects/${projectId}/checkpoint/download`);
const blob = await response.blob();
const url = window.URL.createObjectURL(blob);
const a = document.createElement("a");
a.href = url;
a.download = `lora_model_${Date.now()}.zip`;
a.click();
window.URL.revokeObjectURL(url);
```

（文件后缀目前仍然用 `.zip`，后端实际是 `.pt` 权重，如果你希望更严谨，可以改名为 `.pt`。）

---

### 四、测试与当前运行状态

- **前端：** 已成功执行 `cd read-rhyme && npm run build`，说明 TypeScript 和打包阶段没有错误，LoRA 页面编译通过。
- **后端：**
  - 我尝试在 `backend` 目录跑 `pytest`，但你的本地环境没有设置 `PYTHONPATH=.` 或等效配置，导致测试框架找不到 `app` 模块：
    - `ModuleNotFoundError: No module named 'app'`
  - 这属于环境问题，而非代码错误。你可以在本地这样跑测试：
    - 在 shell 中先执行：`export PYTHONPATH=/Users/gongfan/Documents/youshengshu/backend`
    - 或在 `backend/pyproject.toml` / IDE 里配置好 `src` / `app` 根路径后再跑 `pytest`。

---

### 后续建议

- **如果你希望 LoRA 真正基于“上传的语料”训练：**
  - 在后端 `lora_training.py` 里新增一个专门处理 `UploadFile` 的接口，比如 `/api/lora/projects/{project_id}/train-with-files`，保存上传音频到某个数据集目录，再把这些路径传给 `LoRATrainingService.create_training_job`。
  - 然后前端 `LoRATraining.tsx` 的 FormData 上传逻辑就可以切换到这个新接口，而不是当前的 JSON 调用。

- **如后续在 TTS/音频生成中发现具体错误（例如某个角色声音不对、某个 chunk 一直生成失败）**，可以告诉我具体 projectId / chunkId / 操作步骤，我可以再针对性排查那几条 API 和前端调用链。
