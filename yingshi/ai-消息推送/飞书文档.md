检查飞书 API 调用，定位 400 错误原因：


[3 tools called]


检查飞书 API 调用，修复 400 错误。可能是创建文档的参数格式不正确。


[2 tools called]


修复创建文档的 API 调用：改进参数格式和错误处理。


[3 tools called]


修复代码中的问题：


[3 tools called]


修复错误处理逻辑，避免重复抛出异常：


[2 tools called]


检查飞书 API 文档，确认创建文档的正确格式。可能还需要检查权限问题：


[3 tools called]


检查飞书 API 文档，确认创建文档的正确格式。可能需要在请求体中包含 title，或调整参数格式：


[2 tools called]


改进代码，修复创建文档的参数格式，并增强错误处理：


[2 tools called]


简化代码并检查飞书 API 响应格式，获取更详细的错误信息：


[3 tools called]


检查服务状态，并验证错误信息是否正确传递：


[3 tools called]


重新启动服务，并确保错误信息正确传递：


[3 tools called]


已修复错误处理，并改进了代码。主要修改如下：

## 已完成的修复

1. **改进错误处理** (`rssant_api/feishu_service.py`)：
   - 在 `_make_request` 中增强错误处理，记录详细的错误信息
   - 捕获 HTTP 400 错误并解析飞书 API 返回的错误消息
   - 提供更明确的错误提示

2. **修复创建文档的参数格式**：
   - 当 `folder_token` 为空或无效时，不传 `folder_id` 参数
   - 简化参数处理逻辑

3. **改进错误信息传递** (`rssant_api/views/story.py`)：
   - 确保 ValueError 的错误信息正确传递到前端
   - 返回 400 状态码以便前端识别

4. **重启后端服务**：
   - 已重启后端 API 服务，新代码已生效

## 可能的原因

400 错误通常由以下原因导致：
1. 权限不足：应用缺少 `docx:document` 或 `drive:drive` 权限
2. 应用未发布：应用需要在飞书开放平台发布或获得企业授权
3. 参数格式错误：`folder_token` 格式不正确（已修复）

现在错误信息会更详细，请再次测试导出功能。如果仍有问题，错误提示会显示具体原因，便于进一步排查。

**建议检查**：
1. 在飞书开放平台确认应用已申请并获得了 `docx:document` 和 `drive:drive` 权限
2. 确认应用已发布或已获得企业授权
3. 如果提供了 `folder_token`，确认它是有效的文件夹 token