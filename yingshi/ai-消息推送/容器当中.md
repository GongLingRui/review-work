检查服务运行在哪个容器中：


[12 tools called]


服务运行位置总结：

## 服务运行位置

### 生产环境服务运行在 `rssant` 容器中

**容器信息**:
- **容器名称**: `rssant`
- **镜像**: `rssant:custom`
- **状态**: ✅ 运行中（已运行约 11 分钟）
- **端口映射**: 容器内 80 端口 → 宿主机 6789 端口

### 容器内运行的服务（通过 Supervisor 管理）

所有服务都在同一个 `rssant` 容器内运行：

1. ✅ **nginx** - Web 服务器 + 前端静态文件服务
   - PID: 12, 运行中

2. ✅ **api** - Django API 服务 (Gunicorn)
   - PID: 8, 运行中
   - 监听: `0.0.0.0:6788`

3. ✅ **asyncapi** - 异步 API 服务
   - PID: 9, 运行中

4. ✅ **scheduler** - 任务调度服务
   - PID: 15, 运行中

5. ✅ **worker** - 后台任务处理
   - PID: 17, 运行中

6. ✅ **postgres** - PostgreSQL 数据库
   - PID: 13, 运行中

7. ✅ **initdb** - 数据库初始化（已完成）

### 其他容器

1. **rssant-postgres** - 独立的 PostgreSQL 容器（6天前启动）
   - 端口: `127.0.0.1:5432->5432/tcp`

2. **batchshort-frontend** - 占用宿主机 80 端口（29小时前启动）
   - 这解释了为什么 80 端口显示的是其他应用

3. **monitor-center-frontend** - 监控中心前端（13天前启动）
   - 端口: 8080, 9090, 12315

### 容器管理命令

```bash
# 查看容器状态
docker ps | grep rssant

# 查看容器日志
docker logs -f rssant

# 查看容器内服务状态
docker exec rssant supervisorctl status

# 进入容器
docker exec -it rssant bash

# 重启容器
docker restart rssant

# 停止容器
docker stop rssant

# 启动容器
docker start rssant
```

### 总结

- 所有生产环境服务都在 `rssant` 容器中
- 服务由 Supervisor 统一管理
- 访问地址: http://192.168.191.85:6789
- 宿主机 80 端口被 `batchshort-frontend` 容器占用（旧系统）

详细查看：
```bash
bash /root/gongfan/rrs-aigc/show-container-info.sh
```
